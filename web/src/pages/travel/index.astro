---
import BaseLayout from '../../layouts/BaseLayout.astro';
import TravelMap from '../../components/TravelMap.astro';
import { getCollection } from 'astro:content';

const entries = (
  await getCollection('travel', ({ data }) => !data.draft)
).sort((a, b) => b.data.publishedAt.getTime() - a.data.publishedAt.getTime());
---

<BaseLayout title="Travel">
  <section class="wide">
    <header class="head">
      <p class="eyebrow">On the road</p>
      <h1>Travel</h1>
      <p class="muted lede">
        Places I've been, mapped. Click a pin to read the post.
      </p>
    </header>

    <TravelMap height="600px" />
  </section>

  <section class="wide list-section">

    {
      entries.length === 0 ? (
        <p class="muted">No travel posts yet.</p>
      ) : (
        <ul class="list">
          {entries.map((entry) => (
            <li>
              <a href={`/travel/${entry.slug}/`}>
                <p class="eyebrow">
                  {entry.data.location.city}, {entry.data.location.country}
                </p>
                <h2>{entry.data.title}</h2>
                {entry.data.excerpt && (
                  <p class="muted">{entry.data.excerpt}</p>
                )}
              </a>
            </li>
          ))}
        </ul>
      )
    }
  </section>
</BaseLayout>

<style>
  .head {
    padding: 3rem 0 2rem;
  }
  .list-section {
    padding-top: 2.5rem;
  }
  .list-section :global(h2.section-label) {
    font-size: var(--step-1);
    margin: 0 0 1rem;
  }
  .head h1 {
    margin: 0.2rem 0 0.4rem;
  }
  .lede {
    font-size: var(--step-1);
  }
  .list {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .list li {
    padding: 1.4rem 0;
    border-bottom: 1px solid var(--rule);
  }
  .list a {
    display: block;
    color: var(--ink);
    text-decoration: none;
  }
  .list h2 {
    font-size: var(--step-2);
    margin: 0.2rem 0 0.4rem;
  }
  .list a:hover h2 {
    color: var(--accent);
  }
</style>
